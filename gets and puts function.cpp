#include<stdio.h>
#include<ctype.h>
int main()
{
	//gets(), puts()
	char name[30];
	printf("Enter name:");
	gets(name); // function to read string from user
	printf("Name:  ");
	puts(name);//function to display string
	
}